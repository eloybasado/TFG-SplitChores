import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tfg.splitchores.ionicPushNotifications',
  appName: 'SplitChores',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
